# Text Similarity Matching Algorithm for Supplier Standardization
## Overview

This module implements a text similarity-based matching algorithm to map raw supplier records to a standardized reference database.

The goal is to resolve inconsistencies in supplier naming caused by:
- Different languages
- Typographical errors
- Non-standard descriptions
